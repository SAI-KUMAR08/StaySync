{
  "buildCommand": "cd client && npm ci && npx vite build && mkdir -p ../dist && cp -r dist/* ../dist/",
  "outputDirectory": "dist",
  "installCommand": "cd client && npm ci",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}

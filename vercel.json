{
  "buildCommand": "cd client && npm ci && npx vite build && cp -r dist ../dist",
  "outputDirectory": "dist",
  "installCommand": "cd client && npm ci",
  "framework": "vite",
  "nodeVersion": "20.x"
}
